import React from "react";
import { Box } from "@material-ui/core";
import { makeStyles } from "@material-ui/core/styles";

export const Gag = (props) => {
    return <Box mt={4}></Box>;
};

export default Gag;
